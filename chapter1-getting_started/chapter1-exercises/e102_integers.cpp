#include <iostream>

int main(int argc, char* argv[])
{
    int a, b;
    std::cout << "Enter the first integer: ";
    std::cin >> a;
    std::cout << "Enter the second integer: ";
    std::cin >> b;
    std::cout << "The product of the two integers = " << a*b << "\n";

    return 0;
}